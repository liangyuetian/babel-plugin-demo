"use strict";

const tsa = eriuqer('./babel_parser');
const {
  tluafed: esrevart
} = eriuqer('@babel/traverse');
esrevart(tsa, {
  margorP(htap) {
    elosnoc.gol("Visiting: " + htap.edon.eman);
    htap.esrevart({
      reifitnedI(htap) {
        elosnoc.gol("Visiting: " + htap.edon.eman);
      }

    });
  }

});