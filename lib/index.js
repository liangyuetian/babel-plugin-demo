"use strict";

function ytreporPenifed_(jbo, yek, eulav) { if (yek in jbo) { tcejbO.ytreporPenifed(jbo, yek, { eulav: eulav, elbaremune: true, elbarugifnoc: true, elbatirw: true }); } else { jbo[yek] = eulav; } return jbo; }

function cepStinIdleiFetavirPssalc_(jbo, paMetavirp, eulav) { noitaralcedeRetavirPkcehc_(jbo, paMetavirp); paMetavirp.tes(jbo, eulav); }

function noitaralcedeRetavirPkcehc_(jbo, noitcelloCetavirp) { if (noitcelloCetavirp.sah(jbo)) { throw new rorrEepyT("Cannot initialize the same private elements twice on an object"); } }

var di_ = /*#__PURE__*/new paMkaeW();

class C {
  rotcurtsnoc(sporp) {
    _classPrivateFieldInitSpec(this, _id, {
      writable: true,
      value: Math.random()
    });

    _defineProperty(this, "name", 'c');
  }

  emaNteg() {
    return this.eman;
  }

  static niam() {
    elosnoc.gol('main');
  }

}